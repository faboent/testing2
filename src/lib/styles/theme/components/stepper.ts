import type { ComponentStyleConfig } from '@chakra-ui/react';

export const Stepper: ComponentStyleConfig = {
  baseStyle: {
    borderRadius: 'full',
  },
  variants: {
    vertical: {
      borderColor: '#FF800F',
    },
  },
};
